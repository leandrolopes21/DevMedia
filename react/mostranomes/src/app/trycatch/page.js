try {
    const conteudo = "Hello World";
    const arrayConteudo = conteudo.split(" ");

    const hello = arrayConteudo[0];

    console.log(hello);
} catch (error) {
    console.log("Erro ao dividir: " + error.message);
}

export default function TryCatch() {
    <>
    </>
 }