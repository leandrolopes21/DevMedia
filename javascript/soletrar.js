// Crie um código para soletrar a variável jogoDoAno.
// Execute o loop enquanto index for menor que a quantidade de letras da variável jogoDoAno.

let melhorJogo = 'Forza Horizon 4';
let index = 0;

do {

    console.log(melhorJogo[index]);
    index++;

} while ( index < melhorJogo.length );