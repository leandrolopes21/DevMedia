let pessoas = ["Leandro", "Lucas", "Marize", "Sonia", "Guilherme", "Teodoro"];
let contador = 0;

while (contador < pessoas.length) {

    let nome_pessoa = pessoas[contador];
    contador++;

    console.log("Nomes: " + nome_pessoa);
}